python3 -m venv venv
source venv/bin/activate
# Python version requirement (informational)
# If you're using pip >= 21.1 with a requirements file, use the following for version pinning:
# Requires pip >= 21.1
python_version >= "3.10"

# Web framework
fastapi==0.110.2
uvicorn[standard]==0.29.0

# Data models
pydantic==2.7.1

# LangChain core
langchain==0.2.1
langchain-community==0.2.1
langchain-core==0.2.1

# LLM (Ollama integration)
ollama==0.1.7

# Utility libraries
httpx==0.27.0
requests==2.32.3

# Optional: JSON parsing safety (if you decide to use later)
python-dotenv==1.0.1
